services:
  worker:
    build: ./worker
    volumes:
      - ./worker:/usr/local/app
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build

volumes:
  go-mod-cache:
  go-build-cache:
